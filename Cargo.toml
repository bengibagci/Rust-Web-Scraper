[package]
name = "Rust-Web-Scraper"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12.12", features = ["blocking", "json"] }
scraper = "0.22.0"
tokio = { version = "1", features = ["full"] }